rule:
  meta:
    name: get proxy via registry
    rule-category: machine-access-control/network-environment-probing/test-for-proxy
    author: moritz.raabe@fireeye.com
    scope: function
  features:
    - and:
      - match: open registry key
      - string: ProxyServer