rule:
  meta:
    name: parse URL
    namespace: communication/http
    authors:
      - "michael.hunhoff@mandiant.com"
    scopes:
      static: basic block
      dynamic: thread
  features:
    - or:
      - api: wininet.InternetCrackUrl
